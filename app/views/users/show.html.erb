<h1 class="text-center mb-4 mt-4"> <%=@user.name%>'s profile </h1>
<%=gravatar(@user,size: 200)%>
<div class="text-center mb-4 mt-4"><%=link_to'Edit Profile', edit_user_path(@user), class:"btn btn-outline-primary"%></div>
<%=render 'articles/article'%>